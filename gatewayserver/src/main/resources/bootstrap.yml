spring:
  main:
    allow-bean-definition-overriding: true
  application:
    name: gateway-server
  profiles:
    # choose profile as per need such as local, dev
    active: local
  config:
    # change the config server url as per env such as local, dev
    import: optional:configserver:http://localhost:8071

